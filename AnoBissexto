#include <stdio.h>
#include <stdlib.h>

int main(){
int ano;  
do{
    printf ("Escolha o ano (1900,2100): ");
    scanf ("%d", &ano);
    }while(ano < 1900 || ano > 2100);
    
    if(ano %4 == 0 && (ano %100 != 0 || ano %400 == 0)){
      printf ("\nO ano %d bissexto.\n", ano);          
    } else {
      printf ("\nAno %d nao bissexto.\n", ano);
    }
                    
return (0);
}
